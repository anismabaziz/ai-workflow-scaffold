# Context

This file is the project's ubiquitous language — the single source of truth for domain terms. Keep definitions tight (one or two sentences), pick one canonical term per concept, and list rejected alternatives under `_Avoid_`. Replace the examples below with terms from your own domain.

## Language

**Order** (order): A customer's request to purchase one or more items. _Avoid_: Purchase, transaction, cart.

**Invoice** (invoice): A request for payment sent to a customer after delivery. _Avoid_: Bill, payment request.

**Customer** (customer): A person or organization that places orders. _Avoid_: Client, buyer, account.

## Language — Billing (example)

Group terms by subdomain when natural clusters emerge. Remove this section if your domain is a single cohesive area — a flat list under `## Language` is fine.

---

This glossary is maintained by `domain-modeling`; update it whenever a term is clarified or a new concept is introduced.
