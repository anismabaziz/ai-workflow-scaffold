# Review replies — example-branch

## General comment

> Reviewer: the error shape change looks right, but I want the envelope
> detection explained once, in the docs, instead of in a code comment.

## Inline comments

**File:** `src/api/client.ts`, lines 54-67

> Reviewer: this comment still reads like the convention is undecided. Once
> the docs land, this should just point at them.

**Reply (when the resolving ticket is implemented):**

**File:** `backend/config/viewsets.py`, lines 32-43

> Reviewer: if we remove the pagination special case here, do we still need
> the `pagination` sister key anywhere?

**Reply (when the resolving ticket is implemented):**

**File:** `backend/config/exceptions/handler.py`, line 41

> Reviewer: renaming `status` to `status_code` — any consumer of error bodies
> that reads `.status` today?

**Reply (when the resolving ticket is implemented):**