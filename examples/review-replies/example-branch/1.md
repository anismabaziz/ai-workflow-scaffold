# Review replies (example-branch)

## General comment

> Reviewer: the AND semantics and the closed tag vocabulary both look right.
> I have three inline notes; none are blocking except the 400 wording.

## Inline comments

**File:** `backend/recipes/filters.py`, lines 18-31

> Reviewer: parsing `tags` with a manual split and then validating each value
> means two error paths. Can the filter backends handle the comma-separated
> list in one pass, so an invalid tag never reaches the query builder?

**Reply (when the resolving ticket is implemented):**

**File:** `frontend/src/pages/search/FilterChips.tsx`, lines 40-57

> Reviewer: the chips write straight into the URL on every toggle. Debounce
> this or you will fire a request per click when someone clears all four tags
> in a row.

**Reply (when the resolving ticket is implemented):**

**File:** `backend/recipes/tests/test_search_filters.py`, line 22

> Reviewer: good coverage of the multi-tag case. Add a test where one tag is
> valid and one is not. The whole request should 400, not silently drop the
> bad one.

**Reply (when the resolving ticket is implemented):**
